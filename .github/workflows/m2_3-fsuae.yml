name: M2.3 FS-UAE qualification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-m2-3:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Historical milestone note
        run: |
          echo "M2.3 is frozen. Re-run from its qualified commit for historical evidence."
          echo "Qualified evidence: GitHub Actions run 34851951467."
