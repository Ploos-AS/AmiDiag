name: M2.2 FS-UAE qualification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-m2-2:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Historical milestone note
        run: |
          echo "M2.2 is frozen. Use qualified run 34848336130 for runtime evidence."
          echo "For a true rebuild, check out the qualified M2.2 commit rather than current main."
