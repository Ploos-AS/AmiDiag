name: M2.1 FS-UAE qualification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-m2-1:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      CROSS: m68k-linux-gnu-
      FS_UAE: xvfb-run -a fs-uae
    steps:
      - uses: actions/checkout@v4
      - name: Historical milestone
        run: |
          echo "M2.1 is frozen. Re-run from its qualified commit for historical evidence."
